{
  "name": "chronoai-marketing-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "dashboard",
    "authentication"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dashboard",
    "dev:auth": "npm run dev --workspace=authentication",
    "build": "npm run build --workspace=dashboard",
    "build:auth": "npm run build --workspace=authentication",
    "lint": "npm run lint --workspace=dashboard",
    "lint:auth": "npm run lint --workspace=authentication",
    "preview": "npm run preview --workspace=dashboard",
    "preview:auth": "npm run preview --workspace=authentication"
  }
}

